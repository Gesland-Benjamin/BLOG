.env
node_modules
.env.production.bak
# Ignore user uploads (persisted on server, not in repo)
public/uploads/
.env.*
!.env.example
*.log
logs/
.private-uploads/
coverage/

# Local production audit captures must not be committed.
docs/seo/controle-public-*.json
