<% const _count = (carouselItems || []).length; %> <% if (_count === 0) { %>
<div class="alert alert-secondary">
  Aucun article pour le moment.
</div>
<% } else { %>
<div
  id="mixedCarousel"
  class="smeg-frame carousel slide"
  data-bs-ride="false"
  data-bs-pause="false"
  data-bs-keyboard="true"
  data-bs-touch="true"
  data-bs-wrap="true"
>
  <div class="carousel-inner">
    <% carouselItems.forEach((item, idx) => { %>
    <div class="carousel-item <%= idx === 0 ? 'active' : '' %>">
      <div class="main-carousel-article-card">
        <div class="main-carousel-text">
          <h1 class="main-carousel-title"><%= item.titre %></h1>
          <p class="main-carousel-excerpt"><%= item.extrait %>...</p>
          <p class="main-carousel-author">Par <%= item.auteur %></p>
          <p class="main-carousel-link">
            <a href="/article/<%= item.id %>"
              >Lire l'article complet...</a
            >
          </p>
        </div>
        <div class="main-carousel-image-wrapper">
          <% if (item.image) { %>
            <img
              src="<%= item.image %>"
              alt="<%= item.titre %>"
            />
            <% } else { %>
            <div class="main-carousel-placeholder">
              Aucune image
            </div>
            <% } %>
        </div>
      </div>
    </div>
    <% }) %>
  </div>
    
  </div>
  <% } %>
</div>
